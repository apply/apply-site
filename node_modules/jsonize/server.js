var server = require('router').create();
var jsonify = require('jsonify');

server.options(jsonify);

server.get('/', jsonify(function(request, respond) {
	respond({det:'virker'});
}));
server.post('/', jsonify(function(request, data, respond) {
	respond(data);
}));

server.listen(22221);