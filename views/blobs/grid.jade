- // HARDCODED
- var blob = {_id: 12, name: 'People'}
- var fields = ['Name', 'Description']
- var items = [{_id: 1, fields: [{name: 'Name', value: 'Pau Ramon', type: 'short_text'}, {name: 'Description', value: 'Lorem Ipsum crap', type: 'rich_text'}]}]

h1= blob.name

ul.grid
  - each item in items
    li
    - each field in fields
      div(class= self.getField(item, field).type)= self.getField(item, field).value
